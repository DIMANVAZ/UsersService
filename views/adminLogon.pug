include head
div(id="mainDiv")
    p Войдите в админ-панель с паролем
    form(enctype="application/x-www-form-urlencoded" class="needs-validation" action="/adminPanel" method="POST")
        label(class="form-label") Пароль
        input(id="adminPassField" type="password" name="password" class="form-control" placeholder value='admin' required)
        button(id="adminPassSendButton" type="submit" class="btn btn-md btn-primary") Отправить

    script.
        console.log('!');

    style.
        input,form{
            margin-bottom: 10px;
        }