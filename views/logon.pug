include head
div(id="mainDiv")
    p Войдите в систему или зарегистрируйтесь, используя адрес эл.почты и пароль
    form(action="/pass" enctype="application/x-www-form-urlencoded" method="POST" class="needs-validation")
        label(class="form-label") Email-адрес
        input(type="email" name="email" class="form-control" placeholder value='a@a.a' required)
        label(class="form-label") Пароль
        input(type="password" name="password" class="form-control" placeholder value="user" required)
        button(type="submit" class="btn btn-md btn-primary") Отправить

    a(href='/adminLogon') Для админов

    style.
        input,form{
            margin-bottom: 10px;
        }
